from datetime import datetime, timezone, timedelta

LIMIT                   = 1000
DATE_TYPE               = '%Y/%m/%d %H:%M:%S'
HEARTBEAT_DURATION      = timedelta(days = 1, minutes = -45)
LICENSE_EXPIRE_WARNING  = timedelta(weeks = 6)
